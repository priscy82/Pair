<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WhatsApp Pairing Code Generator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        
        .header {
            background: #2c3e50;
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
        }
        
        .status.connected {
            background: #27ae60;
            color: white;
        }
        
        .status.disconnected {
            background: #e74c3c;
            color: white;
        }
        
        .content {
            padding: 40px;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #2c3e50;
        }
        
        input[type="text"], input[type="number"] {
            width: 100%;
            padding: 15px;
            border: 2px solid #ecf0f1;
            border-radius: 10px;
            font-size: 16px;
            transition: border-color 0.3s;
        }
        
        input[type="text"]:focus, input[type="number"]:focus {
            outline: none;
            border-color: #3498db;
        }
        
        .input-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        
        .divider {
            text-align: center;
            margin: 20px 0;
            color: #7f8c8d;
            font-weight: 500;
        }
        
        .btn {
            background: #3498db;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s;
            width: 100%;
        }
        
        .btn:hover {
            background: #2980b9;
        }
        
        .btn:disabled {
            background: #bdc3c7;
            cursor: not-allowed;
        }
        
        .alert {
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
        }
        
        .alert.error {
            background: #f8d7da;
            border: 1px solid #f5c6cb;
            color: #721c24;
        }
        
        .alert.success {
            background: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
        }
        
        .codes-section {
            background: #f8f9fa;
            padding: 30px;
            border-top: 2px solid #ecf0f1;
        }
        
        .codes-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }
        
        .code-card {
            background: white;
            padding: 20px;
            border-radius: 10px;
            border: 2px solid #e9ecef;
            text-align: center;
        }
        
        .code-number {
            font-size: 12px;
            color: #6c757d;
            margin-bottom: 10px;
        }
        
        .code-value {
            font-family: 'Courier New', monospace;
            font-size: 24px;
            font-weight: bold;
            color: #2c3e50;
            letter-spacing: 2px;
        }
        
        .file-info {
            background: #e9ecef;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
            font-size: 14px;
            color: #495057;
        }
        
        @media (max-width: 768px) {
            .input-row {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .content {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔐 WhatsApp Pairing Code Generator</h1>
            <div class="status <%= connected ? 'connected' : 'disconnected' %>">
                <%= connected ? '✅ Connected' : '❌ Disconnected' %>
            </div>
        </div>
        
        <div class="content">
            <% if (error) { %>
                <div class="alert error">
                    <strong>Error:</strong> <%= error %>
                </div>
            <% } %>
            
            <% if (codes && codes.length > 0) { %>
                <div class="alert success">
                    <strong>Success!</strong> Generated <%= codes.length %> pairing codes
                </div>
            <% } %>
            
            <form method="POST" action="/ui/generate">
                <div class="form-group">
                    <label for="combined">Combined Format (233XXXXXXXXX|Count)</label>
                    <input type="text" id="combined" name="combined" placeholder="233123456789|3">
                </div>
                
                <div class="divider">— OR —</div>
                
                <div class="input-row">
                    <div class="form-group">
                        <label for="number">Phone Number</label>
                        <input type="text" id="number" name="number" placeholder="233123456789">
                    </div>
                    <div class="form-group">
                        <label for="count">Count (1-500)</label>
                        <input type="number" id="count" name="count" min="1" max="500" value="1">
                    </div>
                </div>
                
                <button type="submit" class="btn" <%= !connected ? 'disabled' : '' %>>
                    Generate Pairing Codes
                </button>
            </form>
        </div>
        
        <% if (codes && codes.length > 0) { %>
            <div class="codes-section">
                <h2>Generated Codes (<%= codes.length %>)</h2>
                
                <div class="codes-grid">
                    <% codes.forEach((code, index) => { %>
                        <div class="code-card">
                            <div class="code-number">Code <%= index + 1 %></div>
                            <div class="code-value"><%= code %></div>
                        </div>
                    <% }) %>
                </div>
                
                <% if (lastFile) { %>
                    <div class="file-info">
                        <strong>📁 Saved to:</strong> <%= lastFile %>
                    </div>
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html>
